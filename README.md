Desktop Portage Backup
======================

This is a backup of /etc/portage on my machine. To restore, run 
```
cd /etc
sudo rm -r portage/
sudo git clone https://github.com/Renkin42/portage.git
```
